import { type ClassList } from '@builder.io/qwik';
import { Config, Props } from './types';
export declare const cva: <T>(base?: ClassList, config?: Config<T> | undefined) => (props: Props<T>) => ClassList;
