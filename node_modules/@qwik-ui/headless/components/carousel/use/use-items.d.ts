import { type Signal, type QRL } from '@builder.io/qwik';
import { Visible } from './use-visible';
type Params = {
    startAt?: number;
    transition?: number;
    loop?: boolean;
};
type Active = {
    current: Signal<number>;
    isFirst: Signal<boolean>;
    isLast: Signal<boolean>;
};
export type Items = {
    active: Active;
    visible: Visible;
    total: Signal<number>;
    scrollAt: QRL<(index: number) => void>;
    previous: QRL<() => void>;
    next: QRL<() => void>;
};
export declare const useItems: (ref: Signal<HTMLElement | undefined>, params: Params | void) => Items;
export {};
