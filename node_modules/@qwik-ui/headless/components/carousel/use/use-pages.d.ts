import { type QRL, type Signal } from '@builder.io/qwik';
import { Items } from './use-items';
export type Pages = {
    total: Signal<number>;
    ranges: Signal<number[][]>;
    current: Signal<number>;
    previous: QRL<() => void>;
    next: QRL<() => void>;
    scrollAt: QRL<(index: number) => void>;
};
type Params = {
    items: Items;
    loop?: boolean;
    transition?: number;
};
export declare const usePages: (ref: Signal<HTMLElement | undefined>, params: Params) => Pages;
export declare const getPageFromActiveItem: (index: number, ranges: number[][]) => number;
export {};
