import { type Signal, type QRL } from '@builder.io/qwik';
import { Items } from './use-items';
type Params = {
    items: Items;
    loop?: boolean;
    transition?: number;
};
export type Scroll = {
    next: QRL<() => void>;
    previous: QRL<() => void>;
    to: QRL<(index: number) => void>;
};
export declare const useScroll: (ref: Signal<HTMLElement | undefined>, { items, loop, transition }: Params) => {
    to: QRL<(index: number) => void>;
    previous: QRL<() => void>;
    next: QRL<() => void>;
};
export {};
