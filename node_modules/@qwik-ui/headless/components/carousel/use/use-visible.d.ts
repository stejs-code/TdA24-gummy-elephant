import { type Signal } from '@builder.io/qwik';
export type Visible = {
    first: Signal<number>;
    last: Signal<number>;
};
export declare const useVisible: (ref: Signal<HTMLElement | undefined>) => {
    first: Signal<number>;
    last: Signal<number>;
};
